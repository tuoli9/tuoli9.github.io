<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>迁移目标攻击文献阅读</title>
    <url>/2022/03/09/%E8%BF%81%E7%A7%BB%E7%9B%AE%E6%A0%87%E6%94%BB%E5%87%BB%E6%96%87%E7%8C%AE%E9%98%85%E8%AF%BB/</url>
    <content><![CDATA[<h2 id="Towards-transferable-targeted-attack（2020CVPR）"><a href="#Towards-transferable-targeted-attack（2020CVPR）" class="headerlink" title="Towards transferable targeted attack（2020CVPR）"></a>Towards transferable targeted attack（2020CVPR）</h2><p>代码链接：<a href="https://github.com/TiJoy/Towards-Transferable-Targeted-Attack">https://github.com/TiJoy/Towards-Transferable-Targeted-Attack</a></p>
<span id="more"></span> 
<h3 id="摘要"><a href="#摘要" class="headerlink" title="摘要"></a>摘要</h3><p>目标攻击比非目标攻击更难迁移，在本文中分析了两点造成这种困难的原因，1）在迭代攻击中存在梯度消失的现象，在累计动量时，相邻两次成功的噪声过分一致，造成噪声固化；2）目标对抗样本只与目标类别接近还不够，还要远离原始的类别。为解决上述问题引入庞加莱距离（Poincaré  distance），作为相似性度量，使得在迭代攻击中梯度大小自适应以缓解噪声固化。进一步，在用度量学习（metric learning）对目标攻击过程正则化处理，使对抗样本远离真实标签并且获得高迁移目标对抗样本。lmageNet上的实验证实了我们的方法的优越性，在黑盒针对性攻击中，与其他最新技术相比,其攻击成功率平均提高了8%</p>
<h3 id="庞加莱距离度量（Poincar´e-Distance-Metric）"><a href="#庞加莱距离度量（Poincar´e-Distance-Metric）" class="headerlink" title="庞加莱距离度量（Poincar´e Distance Metric）"></a>庞加莱距离度量（Poincar´e Distance Metric）</h3><p>大多数黑盒攻击使用softmax作为损失函数，</p>
<script type="math/tex; mode=display">
\frac{\partial L}{\partial o_{i}}=p_{i}-y_{i}</script><p>$p_i$是预测概率，$y_i$是标签one-hot编码，对于目标攻击，损失函数对softmax输出的向量<strong>$o$</strong>，在迭代攻击中$p_i$逐渐靠近$y_i$，梯度消失，造成噪声固化，用Poincaré  distance替代交叉熵损失可以解决这个问题。</p>
<p>关于Poincaré  distance：</p>
<p>Poincaré  ball上所有点都在一个单位n维$l_2$球上，两点间的距离定义如下：</p>
<script type="math/tex; mode=display">
d(u, v)=\operatorname{arccosh}(1+\delta(u, v))</script><p>其中$u$和$v$是n维欧几里得空间$l_2$范数小于1的两点，$\delta(u,v)$是等距不变量定义如下</p>
<script type="math/tex; mode=display">
\delta(u, v)=2 \frac{\|u-v\|^{2}}{\left(1-\|u\|^{2}\right)\left(1-\|v\|^{2}\right)}</script><p><img src="https://s2.loli.net/2022/03/12/pD5oxiK27eFhbGa.png" alt="image-20220312114031622" style="zoom:80%;" /></p>
<p>图b中任一点到边缘的距离都是$\infty$，图c中，当接近球的表面时，Poincaré distance增长非常快。这意味着当向表面移动时，梯度大小将会增长。</p>
<p>由以上定义，对于one-hot编码的标签$y$，有$\sum_{i}y_i=1$，这表示标签在一个单位$l_1$球上。当$y$是一个未经平滑处理的one-hot标签，有$|y|_2=1$，所以点$y$在Poincaré ball的边缘，这意味着任一点到$y$的距离都是$+\infty$ 。在目标攻击中，我们的目标是减少模型输出的logits和目标标签之间的距离。</p>
<p>但使用Poincaré distance存在另外一个问题，融合的logits不满足$|l(x)|_2&lt;1$，所以本文中logits用$l_1$距离归一化。对于目标标签，任一点到它的距离是+$\infty$ ，这难以优化。将y减去一个非常小的常数$\xi=0.0001$ ，Poincaré distance 度量损失</p>
<script type="math/tex; mode=display">
\mathcal{L}_{P o}(x, y)=d(u, v)=\operatorname{arccosh}(1+\delta(u, v))</script><p>其中$u=l_k(x)/|l_k(x)|_1$，$v=\max \{y-\xi, 0\}$，$l(x)$是融合的logits。在本文中，$l(x)$是融合了多个模型的logits。</p>
<script type="math/tex; mode=display">
l(x)=\sum_{k=1}^{K} w_{k} l_{k}(x)</script><p>$K$是集成模型数量，$l_k(x)$表示第$k$个模型输出的logits，$w_k$是每个模型的权重，$w_k&gt;0$($\sum_{k=1}^{K}w_k=1$) ，在本文中每个模型的权重相等。通过引入Poincaré度量，当且仅当数据接近目标标签时梯度才会变大。</p>
<h3 id="目标攻击三元损失（Triplet-Loss-for-Targeted-Attack）"><a href="#目标攻击三元损失（Triplet-Loss-for-Targeted-Attack）" class="headerlink" title="目标攻击三元损失（Triplet Loss for Targeted Attack）"></a>目标攻击三元损失（Triplet Loss for Targeted Attack）</h3><p>以往的目标攻击生成的对抗样本和原始类别过于接近，这不利于样本的迁移，本文中引入三元损失，三元损失不仅减少对抗样本和目标类别的距离，还增加对抗样本和原始标签的距离，本文使用干净样本的logits $l(x_{clean})$，one-hot编码的目标标签$y_{tar}$和真实标签$y_{true}$构成三元损失：</p>
<script type="math/tex; mode=display">
\begin{aligned} & \mathcal{L}_{\text {trip }}\left(y_{\text {tar }}, l\left(x_{i}\right), y_{\text {true }}\right) \\=& {\left[D\left(l\left(x_{i}\right), y_{\text {tar }}\right)-D\left(l\left(x_{i}\right), y_{\text {true }}\right)+\gamma\right]_{+} . }\end{aligned}</script><p>注意到$l(x_{adv})$没有归一化，本文中使用角距离（angular distance）作为距离度量：</p>
<script type="math/tex; mode=display">
D\left(l\left(x^{a d v}\right), y_{\text {tar }}\right)=1-\frac{\left|l\left(x^{a d v}\right) \cdot y_{\text {tar }}\right|}{\left\|l\left(x^{a d v}\right)\right\|_{2}\left\|y_{\text {tar }}\right\|_{2}} .</script><p>将三元损失加到损失函数，得到整个的损失函数：</p>
<script type="math/tex; mode=display">
\mathcal{L}_{\text {all }}=\mathcal{L}_{P o}\left(l(x), y_{\text {tar }}\right)+\lambda \cdot \mathcal{L}_{\text {trip }}\left(y_{\text {tar }}, l\left(x_{i}\right), y_{\text {true }}\right)</script><p>基于MI-FGSM，本文也使用输入多样性的方法，整体算法流程如下图：</p>
<p><img src="https://s2.loli.net/2022/03/12/1vNZ4OjV2GLAXkl.png" alt="image-20220312140356435"></p>
<h3 id="实验"><a href="#实验" class="headerlink" title="实验"></a>实验</h3><p>实验细节自己看</p>
<p>参考</p>
<p><a href="https://blog.csdn.net/sydukee/article/details/110822003">https://blog.csdn.net/sydukee/article/details/110822003</a></p>
<p><a href="https://www.jianshu.com/p/afaf551d145e">https://www.jianshu.com/p/afaf551d145e</a></p>
<h2 id="On-Generating-Transferable-Targeted-Perturbations（ICCV2021）"><a href="#On-Generating-Transferable-Targeted-Perturbations（ICCV2021）" class="headerlink" title="On Generating Transferable Targeted Perturbations（ICCV2021）"></a>On Generating Transferable Targeted Perturbations（ICCV2021）</h2><p>代码链接：<a href="https://github.com/Muzammal-Naseer/TTP">https://github.com/Muzammal-Naseer/TTP</a></p>
<p>本文提出一种基于生成方法的高迁移目标扰动（transferable targeted perturbations，TTP)，现有的方法依赖于从一个模型到另一个模型的类别边界信息，不利于扰动的迁移。本文的方法匹配扰动图像的”分布”和目标类别，最后提出一种新的目标函数不仅对齐原始图像和目标图像的全局分布，也匹配两种域的局部邻域结构。基于所提出的目标，我们训练一个生成器，可以自适应地合成特定于给定输入的扰动。我们的生成方法与源或目标域标签无关，同时对广泛的攻击设定，始终比最先进的方法表现更好。</p>
<p>主要方法概括：提出一种新的生成训练框架，通过最大化源分布和目标分布在一个预训练的判别器隐空间共同一致性，将源分布映射为目标分布。</p>
<p>主要贡献</p>
<ul>
<li>生成目标迁移：我们提出了一种新的生成方法来学习可迁移的目标对抗扰动。我们独特的训练机制允许生成器在训练期间探索增强的对抗性空间，这提高了推理期间对抗性样本的可转移性。</li>
<li>互相分布匹配：我们的训练方法是基于给定的源分布和目标分布之间的相互一致。因此，我们的方法可以对训练不需要分类边界信息的生成器提供有针对性的指导。这允许攻击者从无监督的特征中学习有针对性的生成扰动，并消除标记数据的成本。</li>
<li>邻域相似性匹配：除了全局分布匹配方面，我们在对抗和目标类样本之间引入批处理邻域相似性匹配目标，以最大化两个分布之间的局部对齐。</li>
</ul>
<h2 id="相关工作"><a href="#相关工作" class="headerlink" title="相关工作"></a>相关工作</h2><p>基于实例的迭代扰动（PGD,MI,DIM,Po-TRIP,FDA-fd,FDA-N，SGM,LinBP），对每一个样本迭代优化，优化费时并且对于每一个样本独立进行，基于生成的方法虽然需要训练但是可以适应样本，仅通过一次前向传播就可以得到对抗样本。</p>
<p>目标全局扰动更具有迁移性，基于迭代的方法单次迭代一张图像，缺少特定类别全局信息。需要目标类别标签，只能应用于有监督情况。</p>
<p><strong>主要迭代攻击（Iterative Attacks）</strong></p>
<ul>
<li><p><a href="https://arxiv.org/abs/1706.06083">PGD</a>攻击因过拟合而导致的低迁移性（ICRL-2018）。</p>
</li>
<li><p><a href="https://arxiv.org/abs/1710.06081">MI</a>引入动量。它在迭代中累积梯度以减少过拟合（CVPR-2018）。</p>
</li>
<li><p><a href="https://arxiv.org/abs/1803.06978">DIM</a>引入输入变换，如填充（padding）或尺度调节（rescaling）以使模式多样化。将其视为减少过拟合的输入空间中的正则项（CVPR-2019）。</p>
</li>
<li><p><a href="https://openaccess.thecvf.com/content_CVPR_2020/papers/Li_Towards_Transferable_Targeted_Attack_CVPR_2020_paper.pdf">Po-TRIP</a>引入了三元损失，以推动对靶标签的对手示例，同时增加与原始标签的距离（CVPR 2020）。</p>
</li>
<li><p><a href="https://arxiv.org/abs/2004.12519">FDA-fd</a>提出了一种在网络的不同层内模拟特征空间中的类别分布的方法。然后从单个最佳层（ICLR 2020）迁移目标扰动。</p>
</li>
<li><p><a href="https://arxiv.org/abs/2004.14861">FDA-N</a>横跨多层和分类器（NeurlPS2020）相适用于FDA-FD。</p>
</li>
<li><p><a href="https://arxiv.org/abs/2002.05990">SGM</a>发现，在反向传播时，从跳跃连接提供更大的梯度权重增加了可迁移性（ICLR 2020）。</p>
</li>
<li><p><a href="https://arxiv.org/abs/2012.03528">LinBP</a>发现线性反向传播可以提高可迁移性（Neurips 2020）。</p>
</li>
</ul>
<p><strong>主要生成攻击（Generative Attacks）</strong></p>
<ul>
<li><a href="https://arxiv.org/abs/1712.02328">GAP</a>提出了通过交叉熵训练生成模型抵抗预训练模型的机制（CVPR 2018）。</li>
<li><a href="https://arxiv.org/abs/1905.11736">CDA</a>提出了通过相对交叉熵训练生成模型抵抗预训练模型的机制（Neurips 2019）。</li>
<li><a href="https://github.com/Muzammal-Naseer/TTP#Citation">TTP</a>提出基于全局分布匹配目标函数，在一个预训练模型的潜在空间匹配源域和目标域。它不依赖于数据注释（标签）或分类边界信息（ICCV 2021）。</li>
</ul>
<h2 id="基于生成的目标攻击"><a href="#基于生成的目标攻击" class="headerlink" title="基于生成的目标攻击"></a>基于生成的目标攻击</h2><h3 id="生成模型"><a href="#生成模型" class="headerlink" title="生成模型"></a>生成模型</h3><p>目标类别$t$，$P$和$Q$分别表示源域和目标域对抗样本，$x_s$ ~ $P$，$x_t$ ~ $P$，扰动的源域数据$P^{‘}$，$x^{‘}$ ~ $P^{‘}$，$x^{‘}=x_s+\delta$，$\mathcal{D}_\psi(x_s)$，$\mathcal{D}_\psi(x^{‘}_s)$，$\mathcal{D}_\psi(x_t)$分别代表潜在分布。</p>
<p>实验框架如下图，$\mathcal{G}_\theta$是生成器，$\mathcal{D}_\theta$是判别器，$\mathcal{G}_{\theta}$学习源域到目标域的映射，使源域图像获得最小改动，对抗扰动$\delta$限制在范数距离$l_{\infty} \leq \epsilon$，这通过一个可微的裁剪操作将无界的生成器$\mathcal{G}_\theta$生成的对抗扰动投影到距$x_s$固定范数距离内实现。</p>
<script type="math/tex; mode=display">
x^{'}_s=clip(min(x_s+\epsilon,max(\mathcal{W}*\mathcal{G}_\theta,x_s-\epsilon))),\tag 1</script><p>$\mathcal{W}$是固定权重的平滑操作，在不破坏$l_{\infty}$范数限制下减少高频信息，使生成器收敛到一个有意义的结果。</p>
<p><img src="https://s2.loli.net/2022/03/11/vtHX12rqaIWn9cD.png" style="zoom:80%;" /></p>
<p>原有的基于生成的方法依赖于判别器的分类边界，攻击者必须可以获得一个大量标签数据训练的判别器，进一步生成器依赖于输入的特定实例特征，缺少对全局目标分布特征的把握，造成低迁移性。</p>
<p>为解决上述问题，本文对目标分布$Q$建模并使在隐层空间$\mathcal{D}_{\psi}$中扰动的源域分布$P^{‘}$更接近于$Q$</p>
<script type="math/tex; mode=display">
\left\| \delta \right\|_{\infty} \le \epsilon,     s.t., \quad \mathcal{D}_\psi(x^{'}_s)\approx \mathcal{D}_\psi(x_t)  \tag 2</script><p>这个全局的目标函数有两个优势，1）减少扰动和目标分布的不匹配程度，为生成器提供一种引导，2）不需要目标类别信息，判别器可以是一种无监督的方式训练得到</p>
<h3 id="分布匹配"><a href="#分布匹配" class="headerlink" title="分布匹配"></a>分布匹配</h3><p>用KL散度表示$p^{‘}$和$Q$之间的共同一致性，判别特征$\mathcal{D}_\psi(x^{‘}_s)$和$\mathcal{D}_\psi(x_t)$的KL散度定义如下</p>
<script type="math/tex; mode=display">
D_{K L}\left(P^{\prime} \| Q\right)=\frac{1}{N} \sum_{i=1}^{N} \sum_{j=1}^{n} \sigma\left(\mathcal{D}_{\psi}\left(\boldsymbol{x}_{s}^{\prime, i}\right)\right)_{j} \log \frac{\sigma\left(\mathcal{D}_{\psi}\left(\boldsymbol{x}_{s}^{\prime, i}\right)\right)_{j}}{\sigma\left(\mathcal{D}_{\psi}\left(\boldsymbol{x}_{t}^{i}\right)\right)_{j}}</script><p>N代表样本数量，n是判别器输出的特征维数，$\sigma$表示sigmoid操作，由于KL散度非对称$D_{K L}\left( P^{\prime} | Q\right)\neq D_{K L}\left(Q | P^{\prime}\right)$，不是一种距离的度量，损失函数定义</p>
<script type="math/tex; mode=display">
\mathcal{L}=D_{K L}\left(P^{\prime} \| Q\right)+D_{K L}\left(Q \| P^{\prime}\right)\tag{3}</script><p>作为一种常规性度量，引入在分布对齐时添加源域样本的增广版本，这能使生成器更关注于特定目标类类别信息，对输入变换更鲁棒。本文中随机使用旋转，裁剪，水平翻转，颜色抖动或者灰度处理变换，从源域$x_s$得到增广样本$\widetilde{x}_s$  。$\widetilde{x}_s$ ~ $\widetilde{P}$送入生成器$\mathcal{G}_{\theta}$ ，扰动的增广样本$\widetilde{x}_s ^{‘}$ ~ $\widetilde{P}^{‘}$ 用公式（1）投影到增广样本附近，目标域样本没有任何增广操作，$\widetilde{x}_s ^{‘}$ 送入判别器计算$ \mathcal{D}_\psi(\widetilde{x}^{\prime}_s)$和$ \mathcal{D}_\psi(x_t)$之间的共同一致性，公式如下</p>
<script type="math/tex; mode=display">
\mathcal{L}^{a u g}=D_{K L}\left(\tilde{P}^{\prime} \| Q\right)+D_{K L}\left(Q \| \tilde{P}^{\prime}\right) \tag 4</script><h3 id="邻域相似度匹配"><a href="#邻域相似度匹配" class="headerlink" title="邻域相似度匹配"></a>邻域相似度匹配</h3><p>上述损失函数没有考虑局部结构，例如一个样本和它增广版本之间的关系，为了扰动的源域和目标类别样本的可靠对齐，提出匹配两种域之间的邻域相似度分布。考虑一批目标域样本$\left\{x_{t}^{i}\right\}_{i=1}^{N}$和一批扰动的源域样本$\left\{x_{s}^{‘i}\right\}_{i=1}^{N}$ ，对于一个训练批次$x_s ^{‘}$，计算相似度矩阵$\mathcal{S}^s$，它的元素编码了原始样本和它的增广版本$\widetilde{x}_s ^ {‘}$之间的余弦相似度。</p>
<script type="math/tex; mode=display">
\mathcal{S}_{i, j}^{s}=\frac{\mathcal{D}_{\psi}\left(\boldsymbol{x}_{s}^{\prime, i}\right) \cdot \mathcal{D}_{\psi}\left(\tilde{x}_{s}^{\prime, j}\right)}{\left\|\mathcal{D}_{\psi}\left(x_{s}^{\prime, i}\right)\right\|\left\|\mathcal{D}_{\psi}\left(\tilde{x}_{s}^{\prime, j}\right)\right\|} \tag 5</script><p>作为对比，对于目标类别样本$x_t$ ，计算样本之间的余弦相似度矩阵$\mathcal{S}^t$</p>
<script type="math/tex; mode=display">
\mathcal{S}_{i, j}^{t}=\frac{\mathcal{D}_{\psi}\left(x_{t}^{i}\right) \cdot \mathcal{D}_{\psi}\left(x_{t}^{j}\right)}{\left\|\mathcal{D}_{\psi}\left(x_{t}^{i}\right)\right\|\left\|\mathcal{D}_{\psi}\left(x_{t}^{j}\right)\right\|} \tag6</script><p>对上面两个相似度矩阵沿着每一行做softmax操作得到概率估计</p>
<script type="math/tex; mode=display">
\overline{\mathcal{S}}_{i, j}=\frac{\exp \left(\mathcal{S}_{i, j}\right)}{\sum_{k} \exp \left(\mathcal{S}_{i, k}\right)}, where, \mathcal{S} \in\left\{\mathcal{S}^{s}, \mathcal{S}^{t}\right\} \tag 7</script><p>为了匹配源域和目标域局部邻域模式，计算$\overline{\mathcal{S}}^s$和$\overline{\mathcal{S}}^t$KL散度作为损失项</p>
<script type="math/tex; mode=display">
\mathcal{L}^{s i m}=\sum_{i, j} \overline{\mathcal{S}}_{i, j}^{t} \log \frac{\overline{\mathcal{S}}_{i, j}^{t}}{\overline{\mathcal{S}}_{i, j}^{s}}+\sum_{i, j} \overline{\mathcal{S}}_{i, j}^{s} \log \frac{\overline{\mathcal{S}}_{i, j}^{s}}{\overline{\mathcal{S}}_{i, j}^{t}} \tag 8</script><h3 id="整体损失函数"><a href="#整体损失函数" class="headerlink" title="整体损失函数"></a>整体损失函数</h3><p>生成器参数通过最小化下面损失更新</p>
<script type="math/tex; mode=display">
\mathcal{L}_{\mathcal{G}}=\mathcal{L}+\mathcal{L}^{a u g}+\mathcal{L}^{sim } \tag 9</script><p>上述损失激励生成器在扰动源域样本时不仅匹配目标分布的全局特征（$\mathcal{L}+\mathcal{L}^{a u g}$），而且基于邻域连通匹配局部信息（$\mathcal{L}^{sim }$）</p>
<h3 id="实验-1"><a href="#实验-1" class="headerlink" title="实验"></a>实验</h3><p>实验细节自己看吧，对比了很多基于迭代和基于生成的模型</p>
]]></content>
      <categories>
        <category>对抗攻击</category>
        <category>目标攻击</category>
      </categories>
      <tags>
        <tag>对抗攻击</tag>
      </tags>
  </entry>
  <entry>
    <title>sz今天依然没自开</title>
    <url>/2022/03/09/sz/</url>
    <content><![CDATA[<h2 id="望sz看到后立马联系我自开"><a href="#望sz看到后立马联系我自开" class="headerlink" title="望sz看到后立马联系我自开"></a>望sz看到后立马联系我自开</h2><span id="more"></span> 
]]></content>
  </entry>
</search>
